name: release
on:
  push:
    branches:
      - master
jobs:
  build:
    uses: homebots/elements/.github/workflows/main.yml@master
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
